name: Build

on:
  pull_request:
    branches:
      - master

  # Note: Images are currently published via Docker Hub automated build

  push:
    branches:
      - master
  workflow_dispatch: {}

jobs:
  build:
    uses: wetransform/gha-workflows/.github/workflows/dockerfile-build.yml@ac5514fee04f1b8ba18483fe8ff82c70fd134505 # v2.5.0
    with:
      image: wetransform/conversion-hale
      tag: ci
